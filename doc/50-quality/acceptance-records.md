# 哈基哈米核心流程手动验收记录 (C1-C6)

更新时间：2026-05-28
状态：已完成初次验收。

本文档记录哈基哈米核心闭环的 8 条关键业务路径验收结果。

## 1. 游客浏览与搜索 (C2)
- **步骤**：打开首页 -> 点击搜索 -> 输入“哈基” -> 切换“鬼畜”标签 -> 点击搜索结果。
- **验收点**：
    - [x] 首页 SSR 渲染正常，首屏有数据。
    - [x] 搜索 URL 携带 `keyword` 和 `tags` 参数，刷新页面状态不丢失。
    - [x] 结果列表展示符合预期（仅展示 `audit_status=1` 且 `is_public=1` 的歌曲）。
- **结论**：通过。

## 2. 用户登录与身份持久化 (C3)
- **步骤**：点击顶部“登录” -> 输入 `listener / 123456` -> 刷新页面 -> 点击“我的” -> 退出登录。
- **验收点**：
    - [x] 登录后顶部头像显示正常，Cookie 中存有 `front_access_token`。
    - [x] 刷新页面后依然保持登录态（身份持久化）。
    - [x] 退出登录后相关私有页面重定向或提示登录。
- **结论**：通过。

## 3. 歌曲详情与基础互动 (C3)
- **步骤**：进入歌曲详情页 -> 点击“收藏” -> 点击“点赞”。
- **验收点**：
    - [x] 未登录状态下点击互动触发 AuthDialog。
    - [x] 登录后收藏按钮状态即时切换。
    - [x] 收藏成功后，在 `/me/collections` 页面可立即查看到该歌曲。
- **结论**：通过。

## 4. 评论与回复闭环 (C3)
- **步骤**：歌曲详情页发表评论 -> 回复某条评论 -> 删除自己的评论。
- **验收点**：
    - [x] 提交评论后列表自动刷新。
    - [x] 二级回复能正确显示在父评论下方。
    - [x] 删除按钮仅在自己的评论上显示，且删除后层级关系保持一致。
- **结论**：通过。

## 5. 创作者投稿闭环 (C4)
- **步骤**：进入 `/upload` -> 上传音频和封面 -> 填写信息 -> 点击“保存草稿” -> 点击“提交审核”。
- **验收点**：
    - [x] 文件格式（mp3/flac）及大小校验有效。
    - [x] 保存草稿后，从 `/me/works` 的草稿列表可继续编辑。
    - [x] 提交成功后页面跳转至作品列表，状态显示为“待审核”。
- **结论**：通过。

## 6. 后台审核与状态同步 (C5)
- **步骤**：管理员进入后台 -> 曲库管理 -> 对刚提交的歌曲点击“驳回”并输入原因 -> 前台查看结果。
- **验收点**：
    - [x] 后台歌曲列表搜索及分页正常。
    - [x] 驳回操作后，前台 `/me/works` 状态即时更新为“驳回”，并能看到驳回原因。
    - [x] 后台审核日志（`music_audit_log`）记录完整。
- **结论**：通过。

## 7. 榜单归档与历史查看 (C2)
- **步骤**：进入周榜页 -> 切换不同的归档期次。
- **验收点**：
    - [x] 后端返回的 `archives` 列表能正确填充下拉框。
    - [x] 切换期次后，榜单列表数据根据 `snapshot_id` 精确加载。
    - [x] 无归档数据时显示优雅的空态。
- **结论**：通过。

## 8. 开放平台与 Webhook 安全 (C6)
- **步骤**：后台生成 API Key -> 使用 Postman 调用 `/songs` 接口 -> 配置测试 Webhook URL -> 触发 Webhook。
- **验收点**：
    - [x] 携带正确 API Key 时接口返回 200，错误时返回 401。
    - [x] Webhook 推送时 Payload 包含正确签名摘要及时间戳。
    - [x] 后台“Webhook 调用日志”能看到推送成功的状态码（200）。
- **结论**：通过。
