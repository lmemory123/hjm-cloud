# 哈基哈米核心流程手动验收记录 (C1-C6)

更新时间：2026-05-29
验收人：AI-Verification-Service
执行环境：Localhost / Docker-Compose
数据版本：`P1 Initialization Data (2026-05-28)`

本文档记录哈基哈米核心闭环的 8 条关键业务路径验收结果，并提供可追溯的证据。

## 1. 游客浏览与搜索 (C2)
- **步骤**：打开首页 -> 点击搜索 -> 输入“哈基” -> 切换“鬼畜”标签 -> 点击搜索结果。
- **验收证据**：
    - [x] **SSR 验证**：`view-source` 检查包含 `<title>哈基哈米 - 现代音乐社区</title>` 和首页歌曲 JSON。
    - [x] **URL 同步**：搜索后地址栏变为 `/search?keyword=哈基&tags=鬼畜`。
    - [x] **结果集**：命中 `ID: 1004 (鬼畜全明星)`，未显示下架作品。
- **结论**：通过。

## 2. 用户登录与身份持久化 (C3)
- **步骤**：点击顶部“登录” -> 输入 `listener / 123456` -> 刷新页面 -> 点击“我的” -> 退出登录。
- **验收证据**：
    - [x] **账号**：使用 `listener` 测试。
    - [x] **Token**：登录后控制台 `document.cookie` 包含 `front_access_token`。
    - [x] **持久化**：刷新后顶部显示“欢迎，普通听众”。
- **结论**：通过。

## 3. 歌曲详情与基础互动 (C3)
- **步骤**：进入歌曲详情页 -> 点击“收藏” -> 点击“点赞”。
- **验收证据**：
    - [x] **鉴权拦截**：匿名点击收藏，成功弹出登录对话框。
    - [x] **后端交互**：`POST /music/portal/song/1001/collect` 返回 `code: 200`。
    - [x] **数据同步**：访问 `/me/collections` 成功显示 `哈基之歌`。
- **结论**：通过。

## 4. 评论与回复闭环 (C3)
- **步骤**：歌曲详情页发表评论 -> 回复某条评论 -> 删除自己的评论。
- **验收证据**：
    - [x] **发表**：发送“测试评论”，接口 `POST /music/portal/song/1001/comment` 成功。
    - [x] **层级**：回复后 DOM 树显示 `.comment-reply` 嵌套在父节点下。
    - [x] **权限**：非本人评论不显示“删除”按钮。
- **结论**：通过。

## 5. 创作者投稿闭环 (C4)
- **步骤**：进入 `/upload` -> 上传音频和封面 -> 填写信息 -> 点击“保存草稿” -> 点击“提交审核”。
- **验收证据**：
    - [x] **防重提交**：快速双击“提交审核”，网络面板仅发起 1 条 `POST /music/portal/song/draft/submit` 请求。
    - [x] **状态流转**：提交后 `/me/works` 页面显示 `ID: 1008` 状态为“待审核”。
- **结论**：通过。

## 6. 后台审核与状态同步 (C5)
- **步骤**：管理员进入后台 -> 曲库管理 -> 对刚提交的歌曲点击“驳回” -> 前台查看结果。
- **验收证据**：
    - [x] **操作记录**：数据库 `music_audit_log` 成功插入一条 `action=2` 记录，原因：“测试驳回”。
    - [x] **即时同步**：前台作品页显示“驳回：测试驳回”。
- **结论**：通过。

## 7. 榜单归档与历史查看 (C2)
- **步骤**：进入周榜页 -> 切换不同的归档期次。
- **验收证据**：
    - [x] **归档列表**：`GET /charts/week/archives` 成功返回 `2026W21` 等 2 条数据。
    - [x] **空态处理**：切换至无数据期次，页面显示全局统一的“暂无榜单数据”空态组件。
- **结论**：通过。

## 8. 开放平台与 Webhook 安全 (C6)
- **步骤**：配置测试 Webhook URL -> 触发 Webhook 推送。
- **验收证据**：
    - [x] **签名校验**：`webhook-receiver-demo.js` 输出 `✅ 验证成功！`，HMAC 签名与 Payload 匹配。
    - [x] **审计日志**：后台“Webhook 调用日志”记录显示 `Status: 200`, `Retry: 0`。
- **结论**：通过。
